{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && bun run --filter @workspace/api-client-react run typecheck && bun run --filter @workspace/api-zod run typecheck"
  },
  "devDependencies": {
    "orval": "^8.5.2"
  }
}