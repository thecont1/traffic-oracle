{
  "name": "trafficoracle",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "artifacts/blr-traffic"
  ],
  "scripts": {
    "dev": "bun --filter @workspace/blr-traffic run dev",
    "build": "bun --filter @workspace/blr-traffic run build",
    "typecheck": "tsc -p artifacts/blr-traffic/tsconfig.json --noEmit"
  }
}
